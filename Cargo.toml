[package]
name = "localhost"
version = "0.1.0"
edition = "2024"

[dependencies]
httparse = "1.10.1"
libc = "0.2"
serde = { version = "1.0.228", features = ["derive"] }
serde_json = "1.0.149"

[profile.release]
opt-level = 3
lto = true
